/** فاصله‌گذاریِ یکنواخت (مضربِ ۴) و شعاع‌ها. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
} as const;

/** شعاع‌های گردترِ مدرن. */
export const radius = {
  sm: 12,
  md: 16,
  lg: 20,
  xl: 26,
  pill: 999,
} as const;
