diff --git a/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/PaymentActivity.kt b/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/PaymentActivity.kt
index 11810a7..5faf078 100644
--- a/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/PaymentActivity.kt
+++ b/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/PaymentActivity.kt
@@ -13,9 +13,29 @@ class PaymentActivity : ComponentActivity() {
   override fun onCreate(savedInstanceState: Bundle?) {
     super.onCreate(savedInstanceState)
 
+    // Guard against process death mid-payment. This activity keeps its arguments
+    // (command/productId/payment/promise) in static companion fields, so if Android
+    // kills the app while the user is in the Bazaar payment flow, the activity is
+    // recreated in a fresh process with those fields uninitialized and the `when
+    // (command)` below throws UninitializedPropertyAccessException.
+    //
+    // There is no promise left to settle here — the JS side died with the process —
+    // so the only sane move is to close quietly. The purchase itself is not lost:
+    // it stays unconsumed in Bazaar and the app's restore sweep credits it on the
+    // next launch.
+    if (!argsReady) {
+      finish()
+      return
+    }
+
     val callback: PurchaseCallback.() -> Unit = {
       purchaseSucceed { purchaseEntity ->
-        promise.resolve(purchaseEntity.originalJson)
+        // Return the exact signed payload AND its signature so the server can verify
+        // it with the RSA public key (no OAuth). originalJson must stay byte-identical.
+        val result = com.facebook.react.bridge.Arguments.createMap()
+        result.putString("originalJson", purchaseEntity.originalJson)
+        result.putString("dataSignature", purchaseEntity.dataSignature)
+        promise.resolve(result)
         finish()
       }
       purchaseCanceled {
@@ -64,6 +84,11 @@ class PaymentActivity : ComponentActivity() {
     private var payload: String? = null
     private var dynamicPriceToken: String? = null
 
+    // False until start() populates the fields above. A fresh process resets it to
+    // false, which is exactly how onCreate detects that it was recreated after
+    // process death rather than launched normally.
+    private var argsReady = false
+
     @JvmStatic
     fun start(
       activity: Activity,
@@ -80,6 +105,7 @@ class PaymentActivity : ComponentActivity() {
       PaymentActivity.promise = promise
       PaymentActivity.payload = payload
       PaymentActivity.dynamicPriceToken = dynamicPriceToken
+      argsReady = true
       val intent = Intent(activity, PaymentActivity::class.java)
       activity.startActivity(intent)
     }
diff --git a/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/ReactNativePoolakeyModule.kt b/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/ReactNativePoolakeyModule.kt
index a9aa802..0e1710d 100644
--- a/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/ReactNativePoolakeyModule.kt
+++ b/node_modules/@cafebazaar/react-native-poolakey/android/src/main/java/com/cafebazaar/reactnativepoolakey/ReactNativePoolakeyModule.kt
@@ -96,7 +96,7 @@ class ReactNativePoolakeyModule(reactContext: ReactApplicationContext) : ReactCo
     promise: Promise
   ) {
 
-    check(currentActivity != null) {
+    check(getCurrentActivity() != null) {
       "currentActivity is null"
     }
 
@@ -120,7 +120,7 @@ class ReactNativePoolakeyModule(reactContext: ReactApplicationContext) : ReactCo
     promise: Promise
   ) {
 
-    check(currentActivity != null) {
+    check(getCurrentActivity() != null) {
       "currentActivity is null"
     }
 
diff --git a/node_modules/@cafebazaar/react-native-poolakey/src/index.ts b/node_modules/@cafebazaar/react-native-poolakey/src/index.ts
index 83b40a8..b4f45ed 100644
--- a/node_modules/@cafebazaar/react-native-poolakey/src/index.ts
+++ b/node_modules/@cafebazaar/react-native-poolakey/src/index.ts
@@ -26,10 +26,21 @@ function ensureConnected(): Promise<void> {
   }
 
   if (!isConnecting) {
-    isConnecting = bridge.connect(initedRsaKey).then(() => {
-      isConnected = true;
-      isConnecting = undefined;
-    });
+    // PATCH(nodoost): on failure the latch must be cleared, otherwise the first
+    // rejected connect() is cached in `isConnecting` forever and every later call
+    // (consumePurchase, getPurchasedProducts, even purchaseProduct) rejects
+    // instantly until the process dies. This single line is what silently killed
+    // the purchase-restore sweep in production for weeks.
+    isConnecting = bridge.connect(initedRsaKey).then(
+      () => {
+        isConnected = true;
+        isConnecting = undefined;
+      },
+      (e) => {
+        isConnecting = undefined;
+        throw e;
+      }
+    );
   }
 
   return isConnecting;
@@ -50,7 +61,12 @@ const poolakey = {
   connect(rsaKey: string | null) {
     initedRsaKey = rsaKey || null;
     devConnected++;
-    return ensureConnected().catch(praseError);
+    // PATCH(nodoost): `.catch(praseError)` swallowed failures — praseError
+    // *returns* the error, so a dead connection resolved successfully and callers
+    // proceeded as if connected. Re-throw so `await connect()` actually fails.
+    return ensureConnected().catch((e) => {
+      throw praseError(e instanceof Error ? e : new Error(e + ''));
+    });
   },
   async disconnect() {
     devConnected--;
